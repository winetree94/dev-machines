---
# The official package installs the standard Windows client, which already
# bundles the system tray GUI alongside the service.
- name: Install the Tailscale client (winget)
  ansible.builtin.include_role:
    name: winget
  vars:
    winget_packages:
      - Tailscale.Tailscale
