---
# The Ubuntu 26.04 archive carries CMake 4.x, so apt is the native install path
# and keeps the package on the distribution's supported release line.
- name: Cmake | Install CMake (apt)
  become: true
  ansible.builtin.apt:
    name: cmake
    state: present
