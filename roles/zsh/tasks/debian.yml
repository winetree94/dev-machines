---
- name: Zsh | Install zsh (apt)
  become: true
  ansible.builtin.apt:
    name: zsh
    state: present

- name: Zsh | Set zsh as the default shell
  become: true
  ansible.builtin.user:
    name: "{{ ansible_facts['user_id'] }}"
    shell: /usr/bin/zsh
