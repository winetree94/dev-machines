---
- name: Codex | Install the Codex CLI (Homebrew cask)
  when: "'codex' not in (brew_installed_casks | default([]))"
  community.general.homebrew_cask:
    name: codex
    state: present
  become: false
