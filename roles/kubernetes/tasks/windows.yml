---
- name: Install Kubernetes tooling (Chocolatey)
  chocolatey.chocolatey.win_chocolatey:
    name: "{{ item }}"
    state: present
  with_items:
    - kubernetes-cli
    - kubernetes-helm
    - k9s
