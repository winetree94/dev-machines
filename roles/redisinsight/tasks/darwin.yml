---
# The cask token is hyphenated. "redisinsight" still resolves through the
# cask's old_tokens list, but it is deprecated and will stop working.
- name: Redisinsight | Install RedisInsight (Homebrew cask)
  community.general.homebrew_cask:
    name: redis-insight
    state: present
  become: false
