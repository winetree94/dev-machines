---
# winget has no floating Python ID - only per-minor ones - so this is pinned
# and has to be bumped by hand on each new minor release.
- name: Install Python 3 (winget)
  ansible.builtin.include_role:
    name: winget
  vars:
    winget_packages:
      - Python.Python.3.14
