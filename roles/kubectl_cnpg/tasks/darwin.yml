---
# No windows.yml: the CloudNativePG kubectl plugin has no winget package.
- name: Kubectl cnpg | Install the CloudNativePG kubectl plugin (brew)
  community.general.homebrew:
    name: kubectl-cnpg
    state: present
  become: false
