set -x BW_SESSION "{{ fish_bw_session }}"

set -x GITHUB_PAT "{{ github_pat }}"
set -x GITHUB_USER "{{ github_user }}"
set -x GITHUB_TOKEN "{{ github_pat }}"

set -x OPENAI_API_KEY "{{ openai_api_key }}"

set -x GEMINI_API_KEY "{{ gemini_api_key }}"
